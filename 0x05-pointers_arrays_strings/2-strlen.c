#include "main.h"

/**
 * _strlen - prints the alphabet
 * @s: the number to be checked
 *
 * Return: the size of the string
 */
int _strlen(char *s)
{
	int size;

	size = sizeof(*s);
	return (size);
}
